---
artifact-id: ASV-005
artifact-type: application-service
name: "Learning Service"
version: 0.1.0
status: draft
phase-produced: C
owner-agent: SwA
safety-relevant: false
produced-by-skill: SWA-PHASE-C-APP
last-updated: 2026-04-04
engagement: ENG-001
---

<!-- §content -->

## Learning Service

Named service grouping: APP-003 (LearningStore) + AIF-006 (LearningStorePort).

Implements the full `framework/learning-protocol.md §9` query logic:
1. Metadata filter: match `(phase, artifact_type, domain)` against learning index
2. Graph expansion: follow `related` links for up to 2 extra results when primary filter < 3
3. Optional semantic supplement tier: enterprise corpus ≥ 50 entries + `sqlite-vec` available → top-1 embedding match above 0.75 cosine threshold

Recording: `record()` validates schema (9 rules), assigns `<ROLE>-L-NNN` ID, writes to BaseStore AND durable file, emits `learning.created`, returns ID.

Realises CAP-004 (Knowledge Retention capability).

## Properties

| Attribute | Value |
|---|---|
| Components | APP-003, AIF-006 |
| Query result | Top-5 correction texts |
| Realises | CAP-004 (Knowledge Retention) |
| Governed by | `framework/learning-protocol.md §9` |

<!-- §display -->

### archimate

```yaml
layer: Application
element-type: ApplicationService
label: "Learning Service"
alias: ASV_005
```
